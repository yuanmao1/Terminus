# 0.2.0 执行台账

`docs/v2.0文档.md` 是目标；这份是**实测进度**和**阻塞面**。
数字全部来自量测，不来自回忆。每次里程碑变动时更新。

最后核对：`39583eb`（382/382 Debug + ReleaseSafe，分步 327/55，181 锚点各命中一次，零 `CONNECTION_RESET`）

---

## 一、17 个目标的实测状态

| # | 目标 | 状态 | 依据 |
|---|---|---|---|
| 1 | 不可变 OperationAttempt + ReceiptEvent 账本 | **完成** | `operations` + `operation_events`，exactly-one-terminal 为 DB 不变量 |
| 2 | 每次远程调用有 request_id 和可对账回执 | **部分** | `exec`/`run`/`job`/`write`/`session rm`/`push`/`pull` 有；`job kill`/`job rm` 仍无自己的 operation row |
| 3 | 恒定内存 / >2GiB / resume / 双端 SHA / no-clobber / 原子发布 | **完成（6/6）** | `2009ccc`：恒定内存（16× 尺寸下分配量逐字节相同 305,840）、双端 SHA-256、staging+单次 rename、2GiB 上限消失。`662ff00` `--restart`（3/7 判决）、`582f837` `--resume`（`paused`，exec 后端；scp 无 ranged API 故不可能）、`39583eb` 原子 `--no-clobber`（远端 `ln`、本地 `std.Io.Dir.renamePreserve`；本地竞态由两线程 40 轮真跑，远端为顺序驱动） |
| 4 | binary-safe stdin、字节数/SHA 回执、输出流式上限 | **未开始** | — |
| 5 | 独立远端 supervisor（PID/PGID、双 deadline、TERM→KILL） | **未开始·被锁** | 无 helper 产物；`supervisor.zig:61` `pid_proof = .weak` |
| 6 | 真实终态集合 | **完成** | 含 `indeterminate` 与 `proven_failure`；断连从不推断结果 |
| 7 | 多消费者 cursor 与独立 probe cursor | **未开始** | `probe_cursor` 列已存在，无人写 |
| 8 | 持久化 progress / businessResult / phase / terminal receipt | **部分** | terminal receipt 完成；`latest_progress_json`、`latest_business_result` 列在、无人写 |
| 9 | 不可变 job attempt 与 `job inspect/receipt` | **部分** | `job_attempts` + `job inspect` 有；`job receipt` 无 |
| 10 | 生命周期 Lease、稳定 owner、自动续租、takeover 审计 | **完成** | v12；`control.zig` 单一 barrier；六态 `ClaimState` |
| 11 | memory freshness + verify_cmd 受信边界 | **未开始** | — |
| 12 | 完整 handoff | **未开始** | — |
| 13 | `--argv-json`、`--shell none\|bash\|zsh\|powershell` | **未开始** | 零出现 |
| 14 | 原生 audit 模式 | **未开始·被锁** | 无 `cmd_audit`；`audit` 仅作为注释词出现 |
| 15 | Docker/service 结构化 inspect 与 health wait | **未开始** | 依赖 13 |
| 16 | fail-stop PlanRun / PhaseAttempt | **未开始·被锁** | `plan_runs`/`phase_attempts` 表已存在，无编排代码 |
| 17 | 不导出私钥的 pinned stdio tunnel | **未开始·被锁** | `Client.zig` 的 host-key 验证为 **0** |

**3 完成 / 4 部分 / 10 未开始，其中 4 项被 M0 锁死。**

---

## 二、必须由程序员决定才能继续的（v2.0 文档第 41 行）

1. **remote helper 的安装、升级、兼容模型** — 锁目标 5。这是剩余工作量最大的一块，等于再发一个产物：交叉编译矩阵、完整性校验、`noexec` 回退、arch 探测、升级协商。
2. **host-key pin 的首次信任、轮换、撤销规则** — 锁目标 17。当前 `Client.zig` 握手完即返回，零验证。
3. **audit allowlist 与远端受限 helper 的安全边界** — 锁目标 14。依赖 1。
4. **Plan mutation approval 与 reconciliation 语义** — 锁目标 16。

另外两件不是 M0 但同样属于程序员：

5. **`tag v0.2.0` + `npm publish`** — C 类，对外且不可逆。
6. **隔离远端 e2e** — 需要的 SSH key 只存在于真实 `terminus.db` 里，该库禁止触碰。

---

## 三、M1 状态：基本完成

九张表全部存在（`operations`、`operation_events`、`transfer_checkpoints`、`job_attempts`、`leases`、`plan_runs`、`phase_attempts`、`host_pins`、`retention_rules`），
migration 竞态已修（`BEGIN IMMEDIATE` 后重读 `user_version`），最新版本 v12。

---

## 四、当前支线：destructive-control 加固

不推进里程碑计数，但每一刀都挖出账本里真实的伪成功。已闭合：证据工具自身三个缺陷、单事务化、swept lease、泄漏 lease 可读、
authority owner / target operation 分型、锁前采样时间、被拒 CAS 仍提交 terminal、capability 化（374 → 121 个手工决定）、`proven_failure`、退出码进门禁、
合并两份 SKILL 解析器、claim-aware fatal 协议、级联删除只剩合同一条路（`sessions.remove` 已删，短路是编译错误）、
两个销毁合同各自命名且级联集合从 schema 推导、写终态前的续租进门禁、19 处悬空 `§N`。

**队列已清空。** 下一步是 M3。

### 这条支线最后暴露的架构级问题（都已闭合）

1. **"一个销毁合同"是过度声明。** 存在两个:**claim-backed**(`commitDestruction`,持 lease,事务内重读自己的 claim)
   和 **quiescence-backed**(`servers.removeLocked`,不持任何东西,要求整台服务器无人持有任何东西)。
   后者更严,且把它折进前者需要为 server 级动作发明一个 lease。两者互不包含,现已各自命名。
2. **级联集合是隐式的。** `servers` 级联 **7** 张表(不是我先前 grep 出的 10——那是**有 `server_id` 列**的表数,
   其中 3 张是 `SET NULL`)。集合现在从**已应用的 schema** 推导并双向held,新增级联表会点名失败。
3. **两份手写"谁持 claim"清单彼此分叉。** `claim_reporting_bodies` 说 7 个,`claim_holding_bodies` 说 2 个,
   `reportFinishedDuringKill` 在前者不在后者——于是它续租、写终态,而两条续租规则都不看它。现已改为**推导**(函数体内有 `stillOurs(` 即持 claim),
   两份手写清单删除,只留计数;并加了反向不变量(动手的函数体必须也在发布清单里,否则 `ClaimHolderDoesNotReport`)。
4. **写终态前的续租无门禁。** 三处 `_ = stillOurs(...)`,删掉任一行测试全绿。规则不是邻接而是"续租之后不得有远端往返",
   且必须区分**裸结算**(`settleObserved(`,只凭 `authority`)与**合同背书**(走 `commitDestruction`,事务内重读)——
   否则 `removeSession` 那个最安全的站点会被判违规,规则只能被削弱。两侧计数 `(6,1)` 和 `(0,1)`,从合同背书降级为裸结算现在会双重失败。

### 判断为错误投入、明确不做的

**按文件拆 `cmd_job.zig`**（约 5,000 行生产码 + 2,200 行交错测试，60 个顶层函数）。
问题不在"都在一个文件里"，而在 `killJob`(680 行) 与 `removeJob`(545 行) 是同一个 12 步序列的两份，其中 4 行逐字节相同。
按文件切分会**保留**这份重复只是搬个位置，代价 43 个锚点。第 4 项才是这份重复真正的危险面——把漂移变成不可写比换个文件有用。

---

## 五、需要程序员决定的数据丢失语义（新增，均只报告未改行为）

1. **`server rm` 移走了守护私钥的那道 FK。** `servers.key_id REFERENCES keys(id)` 是 `NO ACTION`，
   所以有服务器引用某 key 时 `key rm` 会被**拒绝**。`server rm` 之后该 key 无人引用，而 `keys.remove` 是一句裸 `DELETE FROM keys`
   ——无事务、无屏障、无确认，材料不可再生。两条路径的消息都不提 keys。
   （更正我先前的说法：`server rm` **不**销毁私钥本身，风险是反向的。）
2. **`redaction_rules` 被 `server rm` 销毁，无屏障、无计数、无消息**，而它是**安全声明**（显式声明的 secret 位置，
   使脱敏不依赖模式猜测）。7 张级联表里唯一三重零保护的一张。
3. **已释放的 lease 历史随服务器消失且不被提及** —— 屏障数的是 claim，不是行。
4. **`host_pins` 不在级联里**（无 `server_id` 列，键是 `(host, port, key_type)`）。同 host:port 重建服务器会**继承** pin，
   所以风险是**过期 pin**而非盲信。当前零生产调用方，在 host-key 验证接上之前是惰性的。

---

## 六、支线闭合后的自主路线

按依赖排，不碰被锁的四项：

- **M3**：ByteStream + ArtifactTransfer（目标 3、4）。**已发现账本与状态机本来就是完整的**——`store/transfers.zig` 3,200 行，15 态 `State`、`verifyResume`、`confirmOffset`、`recordVerifiedHash`、角色化 `dest_*`/`partial_*`，而全树没有任何地方调用 `transfers.create`。所以 M3 是**接生产者**，不是做设计。
  `2009ccc` 接上了 push/pull；`slotmarks` 那项**已闭合**（`transfers.contiguousPrefix` 早已实现，无需从 ringio 采用）。resume 也已闭合（`582f837`）：SHA-256 无法从摘要续算，但**发放续传许可的那一遍读正好是那些字节**，hasher 状态白拿——所以续传段照样推进带前缀摘要的 confirmed offset，而不是只在末尾校验一次。剩并行分块、HTTP fetch。no-clobber 也已闭合（`39583eb`）：Zig 0.16 的 `std.Io.Dir.renamePreserve` 就是那个原语，而它在非 Windows/Linux 平台的后端**本身就是 link-then-unlink**——和远端 `ln` 同一族。ringio 仍待用的是 `pipe`（目标 4 的背压输出）和 `fault.zig`（M6）。**不**引入 `RingIo` 作为 `std.Io`——已实测为可证明的空操作，见 memory。

  **M3 仍无动词的一处**：`execution.recoverCheckpoint` 零 CLI 调用方。`verifying` / `publishing`（owner 停在动作中途）既不可 adopt（无偏移可续）也不可 supersede（无人判定尝试已结束），需要它先把 `verifying → paused`、`publishing → indeterminate_publish` 正规化。`wayThrough` 明说这个版本没有对应动词，而不是拿更近的那个 flag 去近似。
- **目标 2 补齐**：`job kill`/`job rm` 各自的 control operation。实测:`operations.Kind` 的 11 个变体里 **`control` 已存在**且 `session rm`（`cmd_session.zig:447`）在用；`kind` 列只是 `TEXT NOT NULL`，无 CHECK 约束；`killJob`/`removeJob` 里 `execution.begin` **零处**。所以缺的只是接线，不需要新 kind 也不需要 migration。（此前这里写的 `job_control` 是错的，那个 kind 叫 `control`。）
  另注：这两个动词的 authority owner 是自己铸的 control id、`Execution.id()` 是目标 attempt——`AuthorityOwner`/`TargetOperation` 分型就是为此存在的。所以它们已有控制身份，缺的是那个身份的 operation 行。
- **M4**：cursor 分离、marker 跨窗口、progress/businessResult 落库、memory freshness、handoff（目标 7、8、11、12）
- **目标 13**：`--argv-json` + 显式 shell → 解锁 **目标 15**
- **目标 9 补齐**：`job receipt`
- **M6 本地门禁**：单元/状态机/JSON golden、migration 多版本与并发、redaction 负向、>2GiB 峰值内存、任意 offset resume、no-clobber 竞争、ENOSPC、rename 失败

到这里本地门禁应当全绿，然后**停在四份合同和发布前面**。

---

## 七、纪律（已被这条支线反复验证过的）

- 改过 anchored 文件后跑 `python tools/mutate.py --check-anchors`；绿测试看不见死锚点
- 锚点唯一性必须程序化计数，不许肉眼
- 一个 mutation 若被编译器抓住，它什么都没证明——重新推导直到是门禁在失败
- 文档里的值/退出码若无门禁解析，就是无保护的；已因此漏过三次
- 匿名 `test {}` 也是一个 test：聚合器用 `comptime { _ = @import(…) }`，不用 `test {}`
- 每轮 mutation run 后 `.zig-cache` 涨约 7.5 GB；整个删掉，冷编译约 90 秒
- 只说"零 `CONNECTION_RESET`"，不说"零输出痕迹"——有五个测试故意打印诊断
- `zig build test` 输出里那一行 `failed command:` 是**既存**的：它是 Zig 回显产生了 stderr 的那个 runner，
  `git stash` 后在 HEAD 上同样出现一次，两边都 exit 0。不要把它当成失败，也不要当成自己引入的
- **`grep -c` 会把注释数进去。** 本轮我用 `grep -c "_ = stillOurs(...)"` 得到 3 就以为 mutation 没应用，
  其中一处是注释里的引用——树实际上是脏的。数"活的调用点"必须剥掉注释行，正是扫描器自己在防的错误
- **手写清单会分叉。** 同一个问题("谁持 claim")在一个文件里被回答两次，两份答案都对自己的问题成立而彼此不一致。
  能推导就推导（走树、读 schema、扫函数体），只留计数
- **但推导不能取消计数。** 推导的成员资格有一个失效模式：删掉某函数体最后一次续租，它就整个**掉出**集合，规则不再看它。
  抓住这一点的是那个被写下来的数字（3 → 2）
- 只说"非注释改动行为 0"这类话时要机械核验，别凭印象
